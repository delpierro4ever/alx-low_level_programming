#include <stdio.h>

/**
 * main - This is main entry point of the program
 *
 * Return: always zero
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		putchar(i + '0');
		if
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
